# This duplicates the requirements stated in setup.cfg because
# bazel's pip_install rule cannot consume it directly. Please
# keep the two in sync.
ipaddress>=1.0.22
validate-email>=1.3
Jinja2>=2.11.1
protobuf>=3.6.1
